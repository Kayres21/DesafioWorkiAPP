Pasos previos de ejecución :

Instalar las siguientes librerias
pip install fpdf 
pip install psycopg2 
pip install pandas
pip install dotenv

Para ejecutar el script basta con usar el comando:

python script.py 

Lo cual generará un archivo de resultados.pdf
